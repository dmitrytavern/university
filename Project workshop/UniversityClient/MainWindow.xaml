<Window x:Class="UniversityClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UniversityClient"
        mc:Ignorable="d"
        Title="Univsersity Client" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="StatusTextBlock" HorizontalAlignment="Center" VerticalAlignment="Center">Current status: Offline</TextBlock>
        <Button x:Name="ConnectButton" Grid.Row="1" Margin="10,10,10,10" Click="ConnectButton_Click">Connect to localhost:8080</Button>
    </Grid>
</Window>
